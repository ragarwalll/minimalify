import { PACKAGE_NAME, GITHUB_LOCATION } from '../package-details.js';

export const AFTER_ALL = {
    value: 'afterAll',
    description: [
        'Generate awesome static site with',
        `@${PACKAGE_NAME}.`,
        'Contribute at',
        GITHUB_LOCATION,
    ],
};
