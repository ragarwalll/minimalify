export const COMMAND_INIT = 'init';
export const COMMAND_INIT_DESCRIPTION =
    'Initialize the minimalify.config.{js,json} file.';

export const COMMAND_BUILD = 'build';
export const COMMAND_BUILD_DESCRIPTION =
    'Build the project using the minimalify.config.{js,json} file';

export const COMMAND_DEV = 'dev';
export const COMMAND_DEV_DESCRIPTION =
    'Launch the project using the minimalify.config.{js,json} file and watch for changes.';
